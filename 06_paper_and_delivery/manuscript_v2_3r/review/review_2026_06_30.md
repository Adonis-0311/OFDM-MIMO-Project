# Independent submission-readiness re-audit — 2026-06-30

## Verdict

**7.8/10; submission-ready after author metadata insertion. Zero critical scientific-integrity issues.**

## Evidence audit

- Scalar G2, the 210-parameter CDL controller, and deterministic A4 remain explicitly separate executable paths.
- Every new headline number traces to a run manifest and CSV under `05_results/`.
- The matched campaign retains all SNR/L cells and discloses NOMP-inspired accuracy dominance, known-order dense-tensor scope, and runtime cost.
- PARAFAC has matched accuracy; FB-ESPRIT is not assigned fabricated joint accuracy.
- Negative/boundary evidence remains visible: CDL-D, A4 cross-SNR failure, L=16, and hardware-impairment limitations.

## Skeptical reviewer risks

1. The learned scalar is not Pareto-optimal under end-to-end runtime. The revised figure and caption now say this directly; the defensible value is bounded amortization plus separate CDL robustness evidence.
2. NOMP-inspired results use known order and a matched dense sinusoidal generator. The manuscript does not generalize them to sparse-resource CFAR NOMP.
3. Real measurements, trained DeepMIMO physical evaluation, integrated K-stage learning, and multi-target CRLB tightness remain future work.

## Visual and packaging audit

The 10-page IEEEtran PDF compiles with resolved citations/references and no overfull boxes. The new double-column two-panel figure has direct labels, explicit better-direction arrows, a non-dominated frontier, and no text overlap at 140 dpi. The paper fits both current IEEE Systems Journal and TVT initial-submission page ceilings.

## Literature calibration

The positioning was checked against NOMP continuum recovery, NOMP-OFDM-ISAC, DDA-Net, and unified tensor ISAC. These neighbors support the current conservative identity: bounded post-support refinement with matched compute-accuracy disclosure, not a deep-unfolding or accuracy-SOTA claim.

## Remaining non-scientific blocker

Replace anonymous author metadata with names, affiliations, ORCIDs, funding, and the final journal selection before portal upload.
